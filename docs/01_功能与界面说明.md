# 功能与界面说明（简版）

## 1. 左侧（整体可折叠）
- 上部：工作流列表 + 新建/复制/删除
- 下部：运行控制（四种模式）
- 交互：折叠后释放主区域空间

## 2. 中区上部：基础配置（可折叠）
- 字段：工作流名称、图表主题、步骤编排开关、并行配置、通知配置、单脚本配置、文件夹监听
- 交互：编辑后保存

## 3. 中区中部：DAG + 步骤列表
- DAG：默认展开，固定高度
- 步骤列表列：顺序 / 类型 / 名称 / 脚本 / 依赖 / 操作 / 是否前置 / 并行
- 行内操作：依赖/并行/前置快捷设置 + 上下移动

## 4. 中区下部：步骤详情编辑器
- 字段：步骤名称、类型、脚本/文件路径、参数、是否前置、依赖与并行设置、子工作流选择
- 交互：保存本步骤 / 撤销修改

## 5. 右侧：实时日志 + 运行历史（可折叠）
- 实时日志：支持按步骤打开日志
- 运行历史：显示最近记录
